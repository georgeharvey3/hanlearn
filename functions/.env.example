# Copy to functions/.env for a local run. The deploy workflow writes this file
# from the repository secrets, and firebase-functions loads it at deploy time.

# Sentry DSN for the Cloud Functions. Leave it empty to disable reporting;
# the emulator and the test suite run without it.
SENTRY_DSN=

# Sentry environment name for the events from the functions.
SENTRY_ENVIRONMENT=development
